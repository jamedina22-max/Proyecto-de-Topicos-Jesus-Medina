{
  "name": "resilient-fetch",
  "version": "1.0.0",
  "description": "Wrapper avanzado sobre fetch con timeout, reintentos y API limpia",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "tsc --noEmit",
    "example": "npx ts-node example.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "fetch",
    "http",
    "client",
    "retry",
    "timeout",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
